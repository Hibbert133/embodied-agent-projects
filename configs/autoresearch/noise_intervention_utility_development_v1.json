{
  "protocol_id": "noise_intervention_utility_development_v1",
  "protocol_version": 1,
  "study_kind": "noise_stratum_extension",
  "split": "development",
  "task": "MetaWorld push-v3",
  "policy": "SawyerPushV3Policy",
  "seed_start": 410,
  "num_seeds": 20,
  "max_initial_steps": 500,
  "max_verification_steps": 500,
  "recovery_policy_config": "configs/autoresearch/recovery_policy_r1_c1.json",
  "registered_probe": {
    "probe_id": "symmetric_xy_repeated_v1",
    "max_environment_steps": 64,
    "outcome_classifier_threshold": 0.11560838098372882
  },
  "structured_evidence": {
    "minimum_phase_samples": 8,
    "contact_distance_m": 0.08,
    "near_goal_distance_m": 0.08
  },
  "conditions": [
    {"condition_id": "fault_05", "evaluator_mechanism": "stochastic_noise"}
  ],
  "candidates": [
    "probe_grounded_compensation",
    "stochastic_retry"
  ],
  "unavailable_candidate_handling": {
    "abstain_is_executable": false,
    "execute_remaining_candidates": true,
    "paired_utility_population": "operational cases with both registered candidates executable",
    "report_coverage": true
  },
  "registered_agent_visible_features": {
    "phase_inconsistency": "higher predicts retry preference",
    "temporal_uncertainty": "higher predicts retry preference",
    "probe_score": "higher estimated bias std norm predicts retry preference",
    "probe_relative_bias_std": "higher predicts retry preference",
    "probe_mean_estimation_residual": "higher predicts retry preference",
    "probe_sign_disagreement": "one minus dominant-axis sign agreement; higher predicts retry preference"
  },
  "evaluator_label": {
    "name": "retry_preferred",
    "definition": "stochastic_retry strictly outranks probe_grounded_compensation under the preregistered fresh-verification utility order",
    "oracle_only": true,
    "exclude_ties": true,
    "exclude_unavailable_candidates": true
  },
  "random_seed_namespaces": {
    "initial_perturbation": 5001,
    "registered_probe": 5002,
    "paired_verification": 5003
  },
  "utility_order": {
    "primary": "verification status: ACCEPTED > INCONCLUSIVE > REJECTED",
    "accepted_tie_break": "fewer verification steps, then lower final object-goal distance",
    "failed_tie_break": "lower final object-goal distance, then fewer verification steps"
  },
  "analysis": {
    "fit_threshold": false,
    "report_label_prevalence": true,
    "report_feature_medians_by_label": true,
    "report_single_feature_roc_auc": true,
    "report_single_feature_pr_auc": true
  },
  "claim_boundary": "Noise-only development characterization; no selector and no held-out claim",
  "rendering": false,
  "api_calls": 0,
  "overwrite_existing_run": false
}
